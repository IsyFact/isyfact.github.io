name: Documentation Build

on:
  pull_request:
    types: [ opened, synchronize, reopened ]

jobs:
  DocsBuild:
    uses: IsyFact/isy-github-actions-templates/.github/workflows/docs_build_template.yml@feature/ifs-5570-detect-latest-build-better
    secrets:
      ANTORA_TRIGGER_TOKEN: ${{ secrets.ANTORA_TRIGGER_TOKEN }}
