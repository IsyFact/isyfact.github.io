name: Documentation Build

on:
  pull_request:
    types: [ opened, synchronize, reopened ]

jobs:
  DocsBuild:
    uses: IsyFact/isy-github-actions-templates/.github/workflows/docs_build_template.yml@feature/ifs-4962-fail-docs-build-on-fatal-error
    secrets:
      ANTORA_TRIGGER_TOKEN: ${{ secrets.ANTORA_TRIGGER_TOKEN }}
